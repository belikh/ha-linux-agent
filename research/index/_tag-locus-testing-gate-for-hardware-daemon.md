---
title: "Tag: locus-testing-gate-for-hardware-daemon"
id: "_tag-locus-testing-gate-for-hardware-daemon"
type: index
created: 2026-09-05T10:51:25.179652+00:00
updated: 2026-09-05T10:51:25.179652+00:00
---

# Tag: locus-testing-gate-for-hardware-daemon

**7** notes.

- [[apimd]] — API.md — Crane API reference: cargoTest is 'cargoTest :: set -> drv' which sets buildPhaseCargoCommand to 'cargo test --profile release' (CARGO_PROFILE env can alter or empty-quote the profile) and pnameSuffix '-test'; optional attrs cargoExtraArgs (default '--locked') and cargoTestExtraArgs (default '') are appended to the invocation, and ALL other derivation attributes delegate to mkCargoDerivation, which passes nativeBuildInputs/buildInputs straight through to stdenv.mkDerivation (with cargo, rustc, rsync, zstd and crane's hooks auto-appended as native inputs) — so adding mosquitto and dbus to nativeBuildInputs in commonArgs gives tests access to those binaries in the check phase, with pre/postCheck hooks available. cargoClippy sets 'cargo clippy --profile release' with cargoClippyExtraArgs (default '--all-targets', e.g. deny specific lints like -- -D warnings) and auto-appends clippy as a native build input; both consume cargoArtifacts from buildDepsOnly for incremental caching. `evergreen`
- [[github-hassagenthassagent-unofficial-development-project-for-the-hassagent-plat]] — Github Hassagenthassagent Unofficial Development Project For The Hassagent Plat — Stub for [[github-hassagenthassagent-unofficial-development-project-for-the-hassagent-plat]] `draft`
- [[interim-report-testing-gate-for-hardware-daemon]] — Interim report — testing-gate-for-hardware-daemon — Concrete test gate: 7-step ladder — clippy map_or fix, then craneLib.cargoTest wiring the existing 8 pure tests (premise of 3 is stale), sysfs-root-injection + tempfile fixtures, mosquitto-SUBPROCESS harness (testcontainers rejected: no container runtime exists fleet-wide) with 5 defect-named regression tests (broker kill-9+restart re-subscribe, birth republish, SIGTERM offline, hung-backend isolation, zero-payload), private dbus-daemon harness for zbus plumbing, and a runNixOSTest cribbed from nixpkgs' own nixos/tests/mosquitto.nix including mid-test systemctl restart mosquitto; gate = nix flake check, all ingredients already shipped in the flake (mosquitto in devShell, dbus in buildInputs, crane pass-through nativeBuildInputs). `evergreen`
- [[nixos-manual-2]] — NixOS Manual — NixOS manual (stable) 'Calling a test' chapter: outside the nixpkgs repository, out-of-tree tests use 'let pkgs = import <nixpkgs> {}; in pkgs.testers.runNixOSTest { imports = [ ./test.nix ]; defaults.services.foo.package = mypkg; }' — runNixOSTest returns a derivation that runs the test, where test.nix is a test module carrying nodes.<name> (QEMU VMs as NixOS modules) and testScript (Python). The defaults attribute applies NixOS options to all machines (both VMs and containers), letting a flake inject its own package into every node. Machines are implicitly started on first action; testScript has machine.succeed/wait_for_unit/start_all plus copy_from_host(source, target) to copy files from the build sandbox into the guest. interactive is a submodule for config applied only when running via the .driverInteractive attribute (e.g. interactive.nodes.rabbitmq.web ui), confirming normal builds run the test headless in QEMU with no display; driverInteractive is the escape hatch for debugging. `evergreen`
- [[what-happens-during-a-system-switchchaptermd]] — NixOS manual - What happens during a system switch? — NixOS manual chapter (nixpkgs master, current): canonical action order during nixos-rebuild switch — stop units, activation script, restart systemd if needed, reset-failed, daemon-reload, 'Reload systemd user instances (systemctl --user daemon-reload)', restart sysinit-reactivation.target, reload/restart/start units. Documents STC_DISPLAY_ALL_UNITS=1 and STC_DEBUG=1 env vars for verbose switch output. Notably the numbered order shows user-instance reloading as a first-class step, but the manual's summary level does not spell out the per-user restart pass — that lives in stc-ng source and PR #517768. `evergreen`
- [[nixpkgs-reference-manual]] — Nixpkgs Reference Manual — nixpkgs manual (unstable) chapter on testers.runNixOSTest: 'A helper function that behaves exactly like the NixOS runTest, except it also assigns this Nixpkgs package set as the pkgs of the test and makes the nixpkgs.* options read-only.' Documented invocation is pkgs.testers.runNixOSTest ({ lib, ... }: { name; nodes.machine = { pkgs, ... }: {...}; testScript = ''; }) returning a derivation that runs the VM test, with notable attributes nodes (evaluated configs) and driverInteractive (interactive Python session). Crucially it directs out-of-tree/external projects to the NixOS manual 'Calling a test' section (see [[nixos-manual-2]]): the out-of-tree shape is 'let pkgs = import <nixpkgs> {}; in pkgs.testers.runNixOSTest { imports = [ ./test.nix ]; defaults.services.foo.package = mypkg; }' — imports-based module with a defaults block to inject the flake's own package into all nodes. Manual is whole-book (182k words); the runNixOSTest/nixosTest content is at lines ~14350-14430. `evergreen`
- [[mosquittonix]] — mosquitto.nix — nixpkgs ships a full Mosquitto NixOS VM test at nixos/tests/mosquitto.nix: a server node enabling services.mosquitto with three listeners (port 1888 plaintext password auth via password_store/password_file/hashed_store/hashed_file user variants, TLS port 1889 with snakeoil certs and require_certificate, anonymous port 1890 with allow_anonymous), plus two client nodes that use mosquitto_pub/sub CLI tools; testScript waits for mosquitto.service then asserts pub/sub, ACL read/write separation (reader can read $SYS/#, writer cannot), TLS client-cert identity, and anonymous publishing. Directly cribbable for a Rust MQTT agent's NixOS test: the broker is configured entirely through services.mosquitto settings/listeners/users options and the test only needs mosquitto.service readiness plus mosquitto clients in environment.systemPackages. `evergreen`

---
*Auto-generated by hyperresearch. Do not edit manually.*
