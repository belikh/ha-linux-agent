{
 "critic_type": "instruction",
 "structural_mirror_check": "PASS — all 8 required_section_headings present in exact order and wording (lines 5, 29, 57, 80, 110, 146, 168, 186) plus ## Opinionated Synthesis (line 200). No structural findings, no orchestrator restructure required.",
 "checks_passed": [
  "All 8 sub-questions addressed in dedicated sections with sufficient depth",
  "All 4 required_formats delivered: file:line defect table with observed exit codes (0 build / 0 test / 101 clippy); phased ordered roadmap (Phases 0-3 with exit criteria and named acceptance tests); two comparison tables against established agents; fleet deployment architecture recommendation (system service per host, role switch, rollout table)",
  "All scope_conditions honoured: HA-agent-not-monitoring-stack framing explicit (Section 4); 7-host fleet inventory from jupiter-os configs; headless boxes with no graphical session covered at depth (Section 5.B); ground truth from codebase file:line reads and verbatim config quotes throughout",
  "Comparison entities all present: HASS.Agent, Telegraf, Glances, Netdata, node_exporter, HA mobile companion baseline (plus go-hass-agent, lnxlink, halinuxcompanion)",
  "Advocate register honoured: single committed thesis (improve, don't adopt; reliability-fleet-features order), full-strength steel-man in Section 4.A, named flip conditions, no hedging on the thesis; unverified specifics honestly flagged as estimates (20-25% notification risk)",
  "Deep-inference provenance discipline: every audited absence states its search (module-list.nix full-text pass, changelog v4.3.1-v14.15.1 zero-occurrence scan, zero host configs for niri/Plasma, no container runtime in any host configuration, zero signal-handling across crates/)",
  "Readability structural R1-R6 all pass: technical terms defined inline on first use (MQTT, broker, clean session, LWT, D-Bus, sysfs, flake, clippy, polkit, nixpkgs, sops-nix, VFD, linger, QoS 1, KVM); citation density ~2.2 per 1000 chars (138 wikilinks / 63,154 chars); forward-looking analysis with 6-12 month horizon and flip conditions; comparison tables for 3+ entities; section primers present; comparison axes worked across 6+ dimensions",
  "Australian English: behaviour, optimises, synchronises, catalogue, labelled all correct in prose; American spellings only inside citation identifiers and verbatim quotes (exempt)"
 ],
 "findings": [
  {
   "id": "instr-001",
   "severity": "high",
   "location": "Section 1 (Current State: A Verified Reliability Audit) — per-backend coverage. Grep of full draft: 'kde' 0 occurrences; 'gamescope' 1 passing occurrence (line 7 reference-graph sentence); 'lutris' 1 passing occurrence (line 23 unverified-flag sentence)",
   "finding": "The decomposition's Backends category names all ten backends (generic, hardware, niri, kde, zfs, syncthing, headscale, gamescope, lutris, launcher) with required fields 'current behaviour, defects, test coverage'. Seven members get that treatment to file:line, but backend-kde is entirely absent from the draft, and backend-gamescope / backend-lutris appear only as passing mentions with no behaviour or defect statement — 3 of 10 named members essentially undelivered, despite vault evidence existing for each.",
   "evidence": "prompt-decomposition.json entities[13]: {\"name\": \"Backends (generic, hardware, niri, kde, zfs, syncthing, headscale, gamescope, lutris, launcher)\", \"required_fields\": [\"current behaviour\", \"defects\", \"test coverage\"]}. Unused vault evidence: backend-kde hard-fails without a session bus (`Connection::session().await?` at backend-kde/src/lib.rs:41) and has zero fleet consumers — 'no jupiterOS host runs Plasma' (interim-report-fleet-service-model-by-host-class.md, line 50; boundary condition (c) at line 86: the KDE backend 'would need the generic backend's warn-and-continue pattern first'); gamescope's only fleet consumer is callisto's arcade session; lutris is installed via `jupiter.gaming.console` appCatalog but no kiosk configuration exercises its buttons (interim-report-adopt-vs-build-honest-verdict.md, point 5). The KDE hard-fail is load-bearing for Section 5's system-service design — it is the stated boundary condition for hosting a desktop class.",
   "suggested_fix": "In Section 1, after the privilege-workaround paragraph, add one short paragraph delivering the missing per-backend audit: backend-kde hard-fails without a session bus (`Connection::session().await?`, backend-kde/src/lib.rs:41) with zero fleet consumers since no host runs Plasma; backend-gamescope's only consumer is callisto's arcade session (kiosks reach it via shared dashboard-gaming modeSpecs); backend-lutris ships behind the `jupiter.gaming.console` appCatalog but no host exercises its buttons — with gamescope and lutris among the three backends self-flagged 'Unverified in a live environment'. Optionally add one sentence in Section 5.B noting the KDE backend must adopt the generic backend's warn-and-disable degradation before the system-service design can host a desktop, putting the rollout's boundary condition on the record."
  },
  {
   "id": "instr-002",
   "severity": "medium",
   "location": "Section 5.D rollout table, kiosk row ('running, 1-second polling, broken backlight writes') and Section 6's systemd-unit-health paragraph ('the kiosk power services')",
   "finding": "The decomposition requires 'touch-wake/backlight specifics' as a field of the TCxWave kiosk entity. Backlight specifics are covered exhaustively (broken chmod, udev GROUP=\"video\", KOBJ_CHANGE), but the touch-wake surface is never mentioned anywhere in the draft (grep 'wake': 0 matches), so the kiosks' actual power/session wiring — the deployed feature set the Phase 2 role switch must preserve — is summarised incompletely.",
   "evidence": "prompt-decomposition.json entities[11]: {\"name\": \"TCx Wave kiosks (adrastea, amalthea, metis, thebe)\", \"required_fields\": [\"current wiring\", \"touch-wake/backlight specifics\"]}. Unused vault evidence: the launcher `screen-power` profile is fused to `tcxwave-screen-power.service`, polkit-gated for io at tcxwave-touch-wake.nix:287, alongside the customer-display switch and the session mutual-exclusion select (interim-report-fleet-service-model-by-host-class.md lines 50 and 117; interim-report-adopt-vs-build-honest-verdict.md points 3 and 5: 'the fleet's actual deployed feature set... launcher mutual-exclusion groups + backlight-as-light + notify + the generic hardware sensors').",
   "suggested_fix": "Expand the kiosk row of the Section 5.D rollout table (or add one sentence to Section 1's fleet-coverage paragraph) stating the kiosks' actual wiring: the launcher `screen-power` profile fused to `tcxwave-screen-power.service` (the touch-wake/power surface, polkit-gated for io per tcxwave-touch-wake.nix:287), the session mutual-exclusion select, and the customer-display switch — the surfaces the Phase 2 rewrite must preserve when the user unit and sysfs-chmod oneshot are deleted."
  },
  {
   "id": "instr-003",
   "severity": "low",
   "location": "Section 8's Forward-looking paragraph and Section 5.D rollout table (7 hosts, 'closing the 4-of-6 gap to 6-of-7')",
   "finding": "The decomposition's headless host class is '(europa, ganymede-successor)'. europa is covered at depth, but the ganymede-successor — the ROADMAP's always-on/headless host class (mosquitto, the HA VM, n8n) — never appears in the draft (grep 'ganymede': 0 matches), so the one named future host class is implicit in the 'role switch absorbing new host classes' phrasing rather than accounted.",
   "evidence": "prompt-decomposition.json entities[12]: {\"name\": \"Headless hosts (europa, ganymede-successor)\"}. Vault: run scaffold.md lines 102-106 — ganymede is named in ROADMAP.md ('always-on/headless boxes like ganymede (mosquitto, the HA VM, n8n)') but no host directory exists yet; the draft's 'module's role switch absorbing new host classes without code changes' (Section 8, Forward-looking) covers the class generically without naming it.",
   "suggested_fix": "Add one sentence to Section 8's Forward-looking paragraph (or one row to the Section 5.D rollout table): the ROADMAP's ganymede-successor — an always-on headless box planned for mosquitto, the HA VM, and n8n, with no host configuration yet — is the first named future consumer of the `server`/`minimal` role, absorbed by the module's role switch without code changes."
  },
  {
   "id": "instr-004",
   "severity": "low",
   "location": "Section 1, opening paragraph ('The workspace is twelve crates, eleven commits, 43 files...')",
   "finding": "The decomposition pins the audit's time horizon as 'current state as of 2026-09 (repo at commit 8369f44)', but the draft never states the commit hash or as-of date for the audited repo state, so the verified-defect audit — the report's foundational format — is not anchored to a reproducible snapshot.",
   "evidence": "prompt-decomposition.json time_horizons[0]: 'current state as of 2026-09 (repo at commit 8369f44)'; git log confirms 8369f44 is HEAD ('fix(light): switch MQTT light to default schema, fixing Unknown state'). The draft pins the version of every external dependency (rumqttc 0.24.0, go-hass-agent v14.15.1, mosquitto 2.1.2) but not its own audit subject.",
   "suggested_fix": "In Section 1's opening paragraph, after 'twelve crates, eleven commits, 43 files', insert the audit anchor: 'audited at commit 8369f44, September 2026' — one clause making the exit-code and file:line evidence reproducible against the exact snapshot it was measured on."
  }
 ]
}
