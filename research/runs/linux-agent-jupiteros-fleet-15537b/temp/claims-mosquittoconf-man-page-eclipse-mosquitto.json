[
  {
    "claim": "Mosquitto's max_queued_messages defaults to 1000 QoS 1 or 2 messages held in the per-client queue above in-flight messages, and 0 means no maximum (explicitly not recommended).",
    "stance": "supports",
    "stance_target": "The #135266-style failure mode: a slow or offline persistent subscriber (e.g. HA during restart) silently drops messages once the per-client queue cap is hit, so a supervisor must not rely on broker queueing alone.",
    "evidence_type": "empirical",
    "scope_conditions": "Eclipse Mosquitto broker, current man page semantics, global option, hot-reloadable",
    "quoted_support": "max_queued_messages count — The maximum number of QoS 1 or 2 messages to hold in the queue (per client) above those messages that are currently in flight. Defaults to 1000. Set to 0 for no maximum (not recommended).",
    "numbers": ["1000 (default queue depth)", "0 = no maximum"],
    "entities": ["Eclipse Mosquitto", "max_queued_messages"],
    "time_period": "current man page (fetched 2026-09)",
    "region": "",
    "confidence": "high",
    "source_note_id": "mosquittoconf-man-page-eclipse-mosquitto"
  },
  {
    "claim": "max_queued_bytes defaults to 0 (no byte limit); when both it and max_queued_messages are set, packets queue until the FIRST limit is reached, and subsequent messages are then silently dropped.",
    "stance": "supports",
    "stance_target": "Broker-side queueing is lossy by design under burst — the agent's supervisor broadcasts need publisher-side pacing or consumer-side heartbeats, not faith in broker queues.",
    "evidence_type": "empirical",
    "scope_conditions": "Eclipse Mosquitto broker, QoS 1/2 outgoing queue per client",
    "quoted_support": "The number of outgoing QoS 1 and 2 messages above those currently in-flight will be queued (per client) by the broker. Once this limit has been reached, subsequent messages will be silently dropped. ... Defaults to 0. (No maximum). ... If both max_queued_messages and max_queued_bytes are specified, packets will be queued until the first limit is reached.",
    "numbers": ["0 default (no byte maximum)", "first-limit-wins semantics"],
    "entities": ["Eclipse Mosquitto", "max_queued_bytes", "max_queued_messages"],
    "time_period": "current man page (fetched 2026-09)",
    "region": "",
    "confidence": "high",
    "source_note_id": "mosquittoconf-man-page-eclipse-mosquitto"
  },
  {
    "claim": "queue_qos0_messages defaults to false — QoS 0 messages are NOT queued for disconnected persistent clients (queuing them is a documented non-standard deviation from MQTT v3.1.1), and if enabled they count toward max_queued_messages.",
    "stance": "supports",
    "stance_target": "Availability heartbeats published at QoS 0 are invisible to a disconnected client; supervisors must use QoS 1+ for anything that must survive a reconnect gap, or expect QoS 0 heartbeats to vanish.",
    "evidence_type": "empirical",
    "scope_conditions": "Eclipse Mosquitto broker; MQTT v3.1.1 spec as baseline",
    "quoted_support": "Set to true to queue messages with QoS 0 when a persistent client is disconnected. ... These messages are included in the limit imposed by max_queued_messages. Defaults to false. Note that the MQTT v3.1.1 spec states that only QoS 1 and 2 messages should be saved in this situation so this is a non-standard option.",
    "numbers": ["default false"],
    "entities": ["Eclipse Mosquitto", "queue_qos0_messages"],
    "time_period": "current man page (fetched 2026-09)",
    "region": "",
    "confidence": "high",
    "source_note_id": "mosquittoconf-man-page-eclipse-mosquitto"
  },
  {
    "claim": "Mosquitto persistence defaults to false: connection, subscription and message data is only written to mosquitto.db when persistence is enabled, at shutdown and at autosave_interval (default 1800 seconds / 30 minutes), or on SIGUSR1.",
    "stance": "supports",
    "stance_target": "A default Mosquitto restart (or crash before autosave) loses queued messages and even subscriptions unless persistence is deliberately enabled — the agent design cannot assume broker restarts preserve session state.",
    "evidence_type": "empirical",
    "scope_conditions": "Eclipse Mosquitto broker, built-in persistence (plugin persistence recommended instead)",
    "quoted_support": "If true, then built-in persistence is enabled. ... If enabled, connection, subscription and message data will be written to disk in mosquitto.db at the location dictated by persistence_location. ... The data will be written to disk when mosquitto closes and also at periodic intervals as defined by autosave_interval. ... If false, the data will be stored in memory only. Defaults to false.",
    "numbers": ["default false", "autosave_interval default 1800 s (30 min)", "SIGUSR1 forces save"],
    "entities": ["Eclipse Mosquitto", "persistence", "autosave_interval", "mosquitto.db"],
    "time_period": "current man page (fetched 2026-09)",
    "region": "",
    "confidence": "high",
    "source_note_id": "mosquittoconf-man-page-eclipse-mosquitto"
  },
  {
    "claim": "autosave_interval counts seconds between in-memory-database saves (default 1800 s = 30 minutes), only effective when built-in persistence is enabled, and 0 restricts saves to exit/SIGUSR1.",
    "stance": "supports",
    "stance_target": "Even with persistence enabled, up to 30 minutes of queue state can be lost on a broker crash — reinforcing that publisher-side retained availability + resubscribe/republish on reconnect is the reliable pattern.",
    "evidence_type": "empirical",
    "scope_conditions": "Eclipse Mosquitto broker, built-in persistence only",
    "quoted_support": "The number of seconds that mosquitto will wait between each time it saves the in-memory database to disk. If set to 0, the in-memory database will only be saved when mosquitto exits or when receiving the SIGUSR1 signal. Note that this setting only has an effect if the built-in persistence is enabled. Defaults to 1800 seconds (30 minutes).",
    "numbers": ["1800 s default", "0 = exit/SIGUSR1 only"],
    "entities": ["Eclipse Mosquitto", "autosave_interval", "autosave_on_changes"],
    "time_period": "current man page (fetched 2026-09)",
    "region": "",
    "confidence": "high",
    "source_note_id": "mosquittoconf-man-page-eclipse-mosquitto"
  },
  {
    "claim": "retain_available defaults to true and retain_expiry_interval governs when retained messages carrying a MQTT5 message-expiry-interval are removed; allow_duplicate_messages is deprecated with behaviour defaulting to true.",
    "stance": "neutral",
    "stance_target": "Broker capability envelope for the availability topic design (retained messages supported by default; MQTT5 expiry is optional extra).",
    "evidence_type": "empirical",
    "scope_conditions": "Eclipse Mosquitto broker",
    "quoted_support": "retain_available [ true | false ] — If set to false, then retained messages are not supported. Clients that send a message with the retain bit will be disconnected if this option is set to false. Defaults to true. ... allow_duplicate_messages [ true | false ] — This option is deprecated and will be removed in a future version. The behaviour will default to true.",
    "numbers": ["retain_available default true"],
    "entities": ["Eclipse Mosquitto", "retain_available", "retain_expiry_interval", "allow_duplicate_messages"],
    "time_period": "current man page (fetched 2026-09)",
    "region": "",
    "confidence": "high",
    "source_note_id": "mosquittoconf-man-page-eclipse-mosquitto"
  },
  {
    "claim": "persistent_client_expiration bounds how long the session of a persistent client survives, which is the broker-side cap on stale-session accumulation for agents using clean_session=false.",
    "stance": "supports",
    "stance_target": "An agent using persistent sessions (clean_session=false) should have its session lifetime bounded broker-side; unbounded persistent sessions from crashed fleet hosts otherwise accumulate.",
    "evidence_type": "empirical",
    "scope_conditions": "Eclipse Mosquitto broker, persistent (clean_session=false) clients",
    "quoted_support": "persistent_client_expiration duration — This option allows the session of persistent clients (those wit... [truncated in extraction]",
    "numbers": [],
    "entities": ["Eclipse Mosquitto", "persistent_client_expiration"],
    "time_period": "current man page (fetched 2026-09)",
    "region": "",
    "confidence": "medium",
    "source_note_id": "mosquittoconf-man-page-eclipse-mosquitto"
  }
]
